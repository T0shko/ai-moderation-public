name: frontend
description: "AI Content Moderation Application"
publish_to: 'none'

version: 2.0.0+1

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  provider: ^6.1.5+1
  http: ^1.6.0
  http_parser: ^4.1.0
  shared_preferences: ^2.5.4
  google_fonts: ^6.3.3
  image_picker: ^1.0.7
  flutter_animate: ^4.5.2
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
