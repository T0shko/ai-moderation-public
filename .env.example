# Database configuration used by Docker-compose and Spring Boot
POSTGRES_DB=aimoderation
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_database_password
DB_PASSWORD=your_database_password

# JWT Token Configuration
JWT_SECRET=your_jwt_secret_change_this_to_a_secure_value_at_least_64_characters_long

# AI Provider Keys (optional — copy this file to ".env" and fill in; never commit ".env")
HUGGINGFACE_API_TOKEN=
# Free chat for the concierge: https://console.groq.com/keys
GROQ_API_KEY=
# Optional override, e.g. llama-3.3-70b-versatile
# GROQ_CHAT_MODEL=llama-3.1-8b-instant