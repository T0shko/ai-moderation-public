# Contextual family phrases — only flagged when NOT benign (see ContextualTextModeration).
# Full insults remain in phrases-toxic.txt / toxic-words.txt.

# Used for review when combined with hostile words, not instant block alone
bashta ti
баща ти
